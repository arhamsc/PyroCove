import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#ffaa5c",
        },
    },
    typography: {
        allVariants: {
            color: "#49403c",
        },
    },
});

export default theme;
